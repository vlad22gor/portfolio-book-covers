# Logs

- 2026-03-27: Репозиторий `portfolio/` выделен из общего контейнера `book covers`: сайт перенесён в новый repo root без переиспользования старого `site/.git`, а docs/tasks разделены от cover-пайплайна. Причина: дать сайту собственный GitHub workflow, чистые правила и независимую историю развития. Проверки: ручная сверка структуры `portfolio/`, наличия repo-local `README.md`, `docs/`, `tasks/` и workflow `/.github/workflows/deploy-site.yml`.
- 2026-03-27: Зафиксирован active baseline сайта: `Astro + TypeScript + Tailwind`, static-first presentation, `Motion` как primary runtime-motion layer для интерактивных компонентов, без обязательного runtime 3D-viewer. Причина: закрепить production-курс сайта после отделения от cover-пайплайна. Проверки: структура документации и правил синхронизирована внутри `portfolio/`.
- 2026-03-27: Завершена верификация выделенного `portfolio/`: GitHub Pages workflow, docs и asset-scripts работают из нового repo root без подпапки `site/`. Причина: подтвердить, что сайт готов жить как отдельный репозиторий и собираться из корня `portfolio/`. Проверки: `npm ci` (audit сообщил о 7 существующих уязвимостях, не исправлялись в рамках миграции), `npm run astro -- check` (0 errors, 20 hints про deprecated `z` в `src/content.config.ts`), `npm run build` (успешно; собраны `/`, `/cases/essentialism`, `/404`), `rg -n "working-directory:\\s*site|path:\\s*site/dist|cache-dependency-path:\\s*site/package-lock.json|cd site|site/src/|site/\\.git" docs README.md package.json .github/workflows -g '!tasks/**'` (0 совпадений).
- 2026-03-27: Подготовлена первичная публикация репозитория `portfolio-book-covers` на GitHub. Причина: подключить отдельный удалённый репозиторий для сайта-портфолио и активировать GitHub Pages workflow из `main`. Проверки: `git status --short --branch` (локальный `main`, без коммитов, без remote), `gh auth status` (авторизация активна как `vlad22gor`), `npm run astro -- check` (0 errors), `npm run build` (успешно, статические страницы собраны).
- 2026-03-27: Создан GitHub-репозиторий `vlad22gor/portfolio-book-covers`, подключён `origin`, выполнен push `main` и включён GitHub Pages в режиме `workflow`. Причина: завершить первичную публикацию сайта и активировать удалённый deploy pipeline. Проверки: `gh repo create vlad22gor/portfolio-book-covers --public --source=. --remote=origin --push` (успешно), `gh run list -R vlad22gor/portfolio-book-covers --limit 5` (workflow `Deploy Book Covers Site to GitHub Pages` поставлен в очередь), `gh api -X POST repos/vlad22gor/portfolio-book-covers/pages -f build_type=workflow` (Pages URL: `https://vlad22gor.github.io/portfolio-book-covers/`).
- 2026-03-27: Исправлен CI-провал первичного деплоя: build больше не зависит жёстко от локальной внешней папки `../covers/...` при отсутствии source-ассетов на runner. Причина: GitHub Actions падал в `prebuild` на `Missing spread.png`, хотя production `.webp` уже закоммичены в репозиторий. Изменения: `package.json` переведён на wrapper `scripts/ensure-build-assets.mjs`, который локально запускает ingest при наличии source, а в CI проверяет наличие committed runtime-ассетов и продолжает build. Проверки: `gh run view 23623511724 -R vlad22gor/portfolio-book-covers --log-failed` (подтвердил причину падения), `npm run build` (локальный ingest + build успешны), временный сценарий без `../covers/projects/Essentialism/01` + `CI=true npm run build` (успешно с fallback на committed assets), `npm run astro -- check` (0 errors, 20 hints про deprecated `z`).
- 2026-03-27: Обновлён workflow GitHub Pages для раннего перехода с deprecated Node 20 runtime на Node 24 runtime экшенов. Причина: успешный deploy продолжал выдавать GitHub-аннотацию о скором принудительном переводе `actions/checkout`, `actions/setup-node` и `actions/upload-artifact` на Node 24. Изменения: в `/.github/workflows/deploy-site.yml` добавлен `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`, action major-версии подняты до `actions/checkout@v6`, `actions/setup-node@v6`, `actions/upload-pages-artifact@v4`, `actions/deploy-pages@v5`. Проверки-источники: `gh api repos/actions/checkout/releases/latest`, `gh api repos/actions/setup-node/releases/latest`, `gh api repos/actions/upload-pages-artifact/releases/latest`, `gh api repos/actions/deploy-pages/releases/latest`, затем повторный workflow run после push.
- 2026-03-27: Устранён остаточный Node 20 warning от внутреннего `actions/upload-artifact`, который тянулся через `actions/upload-pages-artifact@v4`. Причина: даже после апдейта major-версий workflow продолжал получать GitHub-аннотацию про deprecated Node 20 runtime именно на upload step. Изменения: `/.github/workflows/deploy-site.yml` больше не использует `actions/upload-pages-artifact`; вместо этого `dist` пакуется в `github-pages.tar.gz`, а артефакт `github-pages` загружается через `actions/upload-artifact@v7` (Node 24 по умолчанию), совместимо с требованиями `actions/deploy-pages@v5`. Проверки-источники: README `actions/upload-pages-artifact@v4` (manual artifact format допускается), README `actions/upload-artifact@v7` (`runs.using: node24` начиная с v6), повторный workflow run после push.
- 2026-03-27: Попытка полностью убрать остаточный warning через ручную упаковку Pages-артефакта и `actions/upload-artifact@v7` отклонена. Причина: build проходил, но `actions/deploy-pages@v5` стабильно падал на позднем этапе публикации с общим `Deployment failed, try again later`, хотя артефакт `github-pages` находился и deployment создавался. Итог: workflow возвращён к `actions/upload-pages-artifact@v4` как к единственному подтверждённо рабочему варианту для этого репозитория; при этом апдейт `actions/checkout@v6`, `actions/setup-node@v6`, `actions/deploy-pages@v5` и `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` сохранён. Проверки: два подряд run с ручным `upload-artifact@v7` завершились одинаковым fail на deploy, после revert ожидается возврат к успешному deploy flow.
